spring:
  application:
    name: PachaSuite
  profiles:
    active: dev

app:
  jwt:
    secret: ${JWT_SECRET}
    expiration: 86400000  # 24 horas en ms
  cors:
    allowed-origins: "https://prod-front-pachasuite.onrender.com"
